import 'package:flutter/material.dart';

class Category {
  String? title;
  String? thumbnail;
  Color? bgColor;
  Color? textColor;

  Category({
    this.title,
    this.thumbnail,
    this.bgColor,
    this.textColor,
  });
}
